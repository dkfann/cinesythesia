import React from 'react';
import HomeStyles from '../styles/HomeStyles';

const Home = (props) => {
    return (
        <div className="c-home-title" style={ HomeStyles.title }>Cinesythesia</div>
    )
};

export default Home;
